Index: wireplumber_0.4.7.r6.gef7504b/meson.build
===================================================================
--- wireplumber_0.4.7.r6.gef7504b.orig/meson.build
+++ wireplumber_0.4.7.r6.gef7504b/meson.build
@@ -139,15 +139,4 @@ wireplumber_uninstalled = custom_target(
   command : ['cp', '@INPUT@', '@OUTPUT@'],
 )
 
-if meson.version().version_compare('>= 0.58')
-  builddir = meson.project_build_root()
-  srcdir = meson.project_source_root()
 
-  devenv = environment({
-    'WIREPLUMBER_MODULE_DIR': builddir / 'modules',
-    'WIREPLUMBER_CONFIG_DIR': srcdir / 'src' / 'config',
-    'WIREPLUMBER_DATA_DIR': srcdir / 'src',
-  })
-
-  meson.add_devenv(devenv)
-endif
